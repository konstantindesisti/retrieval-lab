# App database
POSTGRES_USER=lore
POSTGRES_PASSWORD=lore
POSTGRES_DB=lore

# OpenAI
# OPENAI_API_KEY=sk-...

# Optional: if using local embedding model (Ollama)
# EMBEDDING_MODEL=nomic-embed-text
# OLLAMA_HOST=http://host.docker.internal:11434
